# Push

## Push to a remote
```bash
git push origin
```

## Force push a branch
```bash
git push --force origin <branch>
```

## Force push safely
```bash
git push --force-with-lease origin <branch>
```

## Push all tags
```bash
git push --tags
```

## Push a specific tag
```bash
git push origin v1.0
```