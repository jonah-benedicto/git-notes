# Merge

## Merge Branch
```bash
git merge <branch>
```

## Non Fast-Forward Merge
```bash
git merge --no-ff <branch>
```

## Squash Merge
```bash
git merge --squash <branch>
```

## Abort Merge
```bash
git merge --abort <branch>
```

